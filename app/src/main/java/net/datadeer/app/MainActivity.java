package net.datadeer.app;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.util.Log;
import android.widget.TextView;

public class MainActivity /*extends AppCompatActivity*/ {
/*
    final static String TAG = "net.datadeer.app";

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        findViewById(R.id.goto_web).setOnClickListener((e) -> {
            Log.v(TAG,"GOTO chat pressed");
            startActivity(new Intent(MainActivity.this, DeerView.class));
        });
        TextView statusText = findViewById(R.id.mainStatus);

        statusText.setText("");

    }*/
}
